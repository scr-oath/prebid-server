# Reports

Engineering reports and performance evaluations done against this fork of
prebid-server. Each report is a self-contained markdown + HTML pair under
its own subdirectory and links back to the upstream prebid issue it relates
to.

## 2026

- **[Sonic JSON evaluation](./sonic/)** *(2026-04-28, Sheridan Rawlins)* —
  benchmarks `bytedance/sonic` against `json-iterator/go` and
  `encoding/json` on real prebid-server fixtures. Covers settings,
  blockers, rollout plan, and performance data. Linked to upstream
  [issue #3437](https://github.com/prebid/prebid-server/issues/3437).
